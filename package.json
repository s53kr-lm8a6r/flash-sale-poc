{
  "name": "flash-sale",
  "private": true,
  "workspaces": [
    "src/**"
  ],
  "scripts": {
    "lint": "biome check",
    "format": "biome format --write",
    "build:apps": "pnpm --filter backend --filter frontend --filter payment build",
    "build:libs": "pnpm --filter common --filter common-ui build"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:common"
  },
  "packageManager": "pnpm@10.9.0"
}
